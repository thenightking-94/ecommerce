const initial_state = {
    purchase_items: null,
    items_picked: [],
    items_wishlist: []
}

const TransactionReducers = (state = initial_state, actions) => {
    return state;
}


export default TransactionReducers;